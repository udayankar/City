const Settings = () => {
    return (
        <div className="placeholder-page">
            <h1>Settings</h1>
            <p>Notification preferences and account configuration will be available here.</p>
        </div>
    );
};

export default Settings;
